import spacy\nimport sqlite3\n\ndef main():\n    # Download spaCy model\n    try:\n        # Check if the model is already downloaded\n        nlp = spacy.load('en_core_web_sm')\n    except OSError:\n        # Download the model if it is not available\n        import os\n        os.system('python -m spacy download en_core_web_sm')\n        nlp = spacy.load('en_core_web_sm')\n \n    # Create SQLite database\n    conn = sqlite3.connect('not_my_handwriting.db')\n    c = conn.cursor()\n \n    # Create a table if not exists\n    c.execute('''CREATE TABLE IF NOT EXISTS handwriting_data (id INTEGER PRIMARY KEY, text TEXT)''')\n    \n    # Commit changes and close the connection\n    conn.commit()\n    conn.close()\n    print('SQLite database and table created successfully!')\n\nif __name__ == '__main__':\n    main()\n